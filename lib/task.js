function task(argument) {
  // body...
}

// get task info
task.prototype.get = function() {
  
};

// change task status to 
task.prototype.done = function(id) {
  
};

// update task status
task.prototype.update = function(id) {

};

// change task status to doing
task.prototype.doing = function(id) {
  
};

// change task status to error
task.prototype.error = function(id) {
  
};